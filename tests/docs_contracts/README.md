# Docs Contract Tests

This folder contains tests that protect documentation contracts: generated docs,
source inventories, roadmap/doc indexing, and parity between durable docs and
published site inputs.

These tests can fail CI, but a failure usually means a docs/source contract
drifted rather than a shipped component API regressed.
