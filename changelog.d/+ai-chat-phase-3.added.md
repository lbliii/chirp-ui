feat(chat): Real composer — IME-safe send, attachments, suggestions

Add `composer()` with `chirpuiComposer` (Enter-to-send, send/stop toggle, paste/drop),
`attachment_chip()` / `suggestion_chips()` with OOB-driven upload status, and
`docs/patterns/ai-chat.md` documenting the server-side abort contract.
