Bump kida-templates to ≥ 0.4.0 — adopts native error boundaries (`{% try %}`/`{% fallback %}`), list comprehensions, scoped slots (`let:`), and an opt-in partial evaluator. Error boundaries added to `suspense_slot`, `oob_fragment`, `streaming_bubble`, `streaming_block`, and `safe_region`/`fragment_island` — render errors now fall back gracefully instead of crashing the page or silently swallowing OOB swaps. Layout primitives (`grid`, `frame`, `stack`, `cluster`, `block`) refactored from nested ternary chains to list comprehensions for readability.
