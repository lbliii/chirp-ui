Fixed the `/releases/` docs page rendering differently across platforms: a `releases/list.html` theme template shadowed the canonical releases-index renderer (`index.html`'s `is_releases_index` path) under Linux template resolution, producing a bare release timeline without the documentation catalog rail or the promoted "Latest" release card. Removed the shadowing template so `/releases/` deterministically renders the catalog rail + release-card grid on every platform (and the live site, not just the browser-smoke gate). Proven by `test_bengal_release_index_promotes_latest_card`.
